# Copyright © Erickson Lopez. MIT License.
name: Mutation Testing (Stryker)

# ─── Purpose ─────────────────────────────────────────────────────────────────
# Runs Stryker.NET mutation testing across all EricksonLopez.Processes packages:
#   • Core Library (EricksonLopez.Processes)
#   • Abstractions (EricksonLopez.Processes.Abstractions)
#   • DependencyInjection (EricksonLopez.Processes.DependencyInjection)
#   • Generator (EricksonLopez.Processes.Generator)
#   • SystemTextJson (EricksonLopez.Processes.SystemTextJson)
#   • Testing (EricksonLopez.Processes.Testing)
#   • Outbox (EricksonLopez.Processes.Outbox)
#   • Mediator (EricksonLopez.Processes.Mediator)
#   • Events (EricksonLopez.Processes.Events)
#   • Storage: PostgreSql, SqlServer, Sqlite, MySql, MariaDb, Oracle
#   • Analyzers (EricksonLopez.Processes.Analyzers)
#
# Threshold policy (single source of truth defined in stryker-*.json):
#   • high  (≥100%) — ✅ HIGH
#   • low   (≥ 98%) — 🟡 LOW
#   • warn  (≥ 95%) — 🟠 WARNING (approaching break threshold)
#   • break (< 95%) — ❌ FAILED (Stryker exits with non-zero code)
# ─────────────────────────────────────────────────────────────────────────────

on:
  workflow_dispatch:
    inputs:
      mutation-level:
        description: "Stryker mutation level (Basic, Standard, Advanced)"
        required: false
        default: "Standard"
        type: choice
        options:
          - Basic
          - Standard
          - Advanced
  workflow_call:
    inputs:
      mutation-level:
        description: "Stryker mutation level (Basic, Standard, Advanced)"
        required: false
        default: "Standard"
        type: string
  # Weekly scheduled execution (Monday 04:00 UTC) to catch regressions early
  schedule:
    - cron: "0 4 * * 1"

concurrency:
  group: mutation-testing-${{ github.ref }}
  cancel-in-progress: true

env:
  DOTNET_NOLOGO: true
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true

permissions:
  contents: read

jobs:
  mutation-test:
    name: Stryker — ${{ matrix.name }}
    runs-on: ubuntu-latest
    timeout-minutes: 480
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Core
            config: stryker-config.json
            output-dir: StrykerOutput/core
            artifact-name: stryker-report-core
          - name: Abstractions
            config: stryker-abstractions-config.json
            output-dir: StrykerOutput/abstractions
            artifact-name: stryker-report-abstractions
          - name: DependencyInjection
            config: stryker-dependencyinjection-config.json
            output-dir: StrykerOutput/dependencyinjection
            artifact-name: stryker-report-dependencyinjection
          - name: Generator
            config: stryker-generator-config.json
            output-dir: StrykerOutput/generator
            artifact-name: stryker-report-generator
          - name: SystemTextJson
            config: stryker-systemtextjson-config.json
            output-dir: StrykerOutput/systemtextjson
            artifact-name: stryker-report-systemtextjson
          - name: Testing
            config: stryker-testing-config.json
            output-dir: StrykerOutput/testing
            artifact-name: stryker-report-testing
          - name: Outbox
            config: stryker-outbox-config.json
            output-dir: StrykerOutput/outbox
            artifact-name: stryker-report-outbox
          - name: Mediator
            config: stryker-mediator-config.json
            output-dir: StrykerOutput/mediator
            artifact-name: stryker-report-mediator
          - name: Events
            config: stryker-events-config.json
            output-dir: StrykerOutput/events
            artifact-name: stryker-report-events
          - name: StoragePostgreSql
            config: stryker-storagepostgresql-config.json
            output-dir: StrykerOutput/storagepostgresql
            artifact-name: stryker-report-storagepostgresql
          - name: StorageSqlServer
            config: stryker-storagesqlserver-config.json
            output-dir: StrykerOutput/storagesqlserver
            artifact-name: stryker-report-storagesqlserver
          - name: StorageSqlite
            config: stryker-storagesqlite-config.json
            output-dir: StrykerOutput/storagesqlite
            artifact-name: stryker-report-storagesqlite
          - name: StorageMySql
            config: stryker-storagemysql-config.json
            output-dir: StrykerOutput/storagemysql
            artifact-name: stryker-report-storagemysql
          - name: StorageMariaDb
            config: stryker-storagemariadb-config.json
            output-dir: StrykerOutput/storagemariadb
            artifact-name: stryker-report-storagemariadb
          - name: StorageOracle
            config: stryker-storageoracle-config.json
            output-dir: StrykerOutput/storageoracle
            artifact-name: stryker-report-storageoracle
          - name: Analyzers
            config: stryker-analyzers-config.json
            output-dir: StrykerOutput/analyzers
            artifact-name: stryker-report-analyzers

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore dependencies
        run: dotnet restore EricksonLopez.Processes.slnx

      - name: Build (Release)
        run: dotnet build EricksonLopez.Processes.slnx --no-restore --configuration Release

      - name: Install Stryker
        run: dotnet tool install --global dotnet-stryker

      - name: Run Stryker (${{ matrix.name }})
        id: stryker
        run: |
          set +e
          dotnet stryker \
            --config-file ${{ matrix.config }} \
            --test-runner vstest \
            --mutation-level ${{ inputs.mutation-level || 'Standard' }} \
            --output ${{ matrix.output-dir }}
          STRYKER_EXIT_CODE=$?
          echo "exit_code=$STRYKER_EXIT_CODE" >> $GITHUB_OUTPUT
          echo "Stryker finished with exit code: $STRYKER_EXIT_CODE"
          exit 0

      - name: Upload Stryker report (${{ matrix.name }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact-name }}-${{ github.run_id }}
          path: ${{ matrix.output-dir }}/
          retention-days: 30

      # ─── Extract and report mutation score ───────────────────────────────
      - name: Extract and report mutation score (${{ matrix.name }})
        id: report
        if: always()
        run: node scripts/record-stryker-result.js "${{ matrix.output-dir }}" "${{ matrix.name }}" "${{ matrix.config }}"

      - name: Upload summary JSON (${{ matrix.name }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stryker-summary-${{ matrix.name }}
          path: StrykerOutput/summary-${{ matrix.name }}.json
          retention-days: 30

  # ─── Consolidated Gate Aggregation & Commit Status ───────────────────────────
  mutation-gate-summary:
    name: Consolidated Quality Gate & Commit Status
    runs-on: ubuntu-latest
    needs: mutation-test
    if: always()
    permissions:
      contents: write
      statuses: write
      actions: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all summary artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: stryker-summary-*
          path: ./all-summaries
          merge-multiple: true

      - name: Evaluate Consolidated Mutation Quality Gate
        id: eval-gate
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = require('path');

            const summariesDir = path.join(process.cwd(), 'all-summaries');
            let summaryFiles = [];
            if (fs.existsSync(summariesDir)) {
              summaryFiles = fs.readdirSync(summariesDir).filter(f => f.startsWith('summary-') && f.endsWith('.json'));
            }

            console.log(`Found ${summaryFiles.length} summary files in ${summariesDir}`);

            const packages = [];
            let totalKilled = 0;
            let totalMutants = 0;
            let allPassed = summaryFiles.length > 0;
            let breakThreshold = 95;

            for (const file of summaryFiles) {
              try {
                const data = JSON.parse(fs.readFileSync(path.join(summariesDir, file), 'utf8'));
                packages.push(data);
                totalKilled += data.mutants_killed || data.killed || 0;
                totalMutants += data.total_mutants || data.total || 0;
                if (!data.passed_break && !data.passed) {
                  allPassed = false;
                }
                if (data.threshold_break || data.break) {
                  breakThreshold = data.threshold_break || data.break;
                }
              } catch (e) {
                console.error(`Error reading ${file}:`, e.message);
                allPassed = false;
              }
            }

            // Sort packages alphabetically for stable reporting
            packages.sort((a, b) => (a.package || '').localeCompare(b.package || ''));

            const overallScore = totalMutants > 0 ? ((totalKilled / totalMutants) * 100).toFixed(2) : (packages.length > 0 ? "100.00" : "0.00");
            const numScore = parseFloat(overallScore);

            let color = 'red';
            let statusLabel = '❌ FAILED';
            if (allPassed && numScore >= 100) {
              color = 'brightgreen';
              statusLabel = '✅ HIGH';
            } else if (allPassed && numScore >= 98) {
              color = 'green';
              statusLabel = '🟡 LOW';
            } else if (allPassed && numScore >= breakThreshold) {
              color = 'yellow';
              statusLabel = '🟠 WARNING';
            } else {
              color = 'red';
              statusLabel = '❌ FAILED';
              allPassed = false;
            }

            console.log(`Consolidated Score: ${overallScore}%`);
            console.log(`Overall Status   : ${statusLabel}`);
            console.log(`Passed Gate      : ${allPassed}`);

            core.setOutput('overall_score', overallScore);
            core.setOutput('passed', allPassed);

            // Append Consolidated Summary Table to Step Summary
            let md = `\n# 🛡️ Consolidated Stryker Mutation Testing Gate\n\n`;
            md += `| Package | Score | Killed / Total | Status |\n`;
            md += `|---------|-------|----------------|--------|\n`;
            for (const p of packages) {
              md += `| **${p.package}** | **${p.mutation_score || p.score}%** | ${p.mutants_killed || p.killed}/${p.total_mutants || p.total} | ${p.status} |\n`;
            }
            md += `| **OVERALL** | **${overallScore}%** | **${totalKilled}/${totalMutants}** | **${statusLabel}** |\n\n`;
            md += `**Gate Verdict**: ${allPassed ? '✅ PASSED' : '❌ FAILED'} (Break Threshold: ≥${breakThreshold}%)\n`;

            const summaryPath = process.env.GITHUB_STEP_SUMMARY;
            if (summaryPath && fs.existsSync(path.dirname(summaryPath))) {
              fs.appendFileSync(summaryPath, md, 'utf8');
            }

            // Update README.md badge and table
            const readmePath = path.join(process.cwd(), 'README.md');
            if (fs.existsSync(readmePath) && packages.length > 0) {
              let readme = fs.readFileSync(readmePath, 'utf8');

              // 1. Update Mutation Score Badge
              const badgeScoreText = `${numScore}%`;
              const badgeRegex = /\[!\[Mutation Score\]\(https:\/\/img\.shields\.io\/badge\/Mutation_Score-[^)]+\)\]\([^)]+\)/g;
              const newBadge = `[![Mutation Score](https://img.shields.io/badge/Mutation_Score-${encodeURIComponent(badgeScoreText)}-${color}?style=for-the-badge&logo=stryker&logoColor=white)](https://github.com/${context.repo.owner}/${context.repo.repo}/blob/main/docs/ci-cd.md)`;
              if (badgeRegex.test(readme)) {
                readme = readme.replace(badgeRegex, newBadge);
              }

              // 2. Update Mutation Score Table
              let tableMd = `<!-- STRYKER_RESULTS_START -->\n`;
              tableMd += `| Package | Mutation Score | Mutants Killed / Total | Quality Gate Status |\n`;
              tableMd += `|:---|:---:|:---:|:---:|\n`;
              for (const p of packages) {
                const pScore = p.mutation_score !== undefined ? `${p.mutation_score}%` : `${p.score}%`;
                const pKilled = p.mutants_killed !== undefined ? p.mutants_killed : p.killed;
                const pTotal = p.total_mutants !== undefined ? p.total_mutants : p.total;
                tableMd += `| **${p.package}** | **${pScore}** | ${pKilled} / ${pTotal} | ${p.status} |\n`;
              }
              tableMd += `| **OVERALL ECOSYSTEM** | **${overallScore}%** | **${totalKilled} / ${totalMutants}** | **${statusLabel}** |\n`;
              tableMd += `<!-- STRYKER_RESULTS_END -->`;

              const tableRegex = /<!-- STRYKER_RESULTS_START -->[\s\S]*?<!-- STRYKER_RESULTS_END -->/;
              if (tableRegex.test(readme)) {
                readme = readme.replace(tableRegex, tableMd);
              }

              fs.writeFileSync(readmePath, readme, 'utf8');
              console.log(`✅ Successfully updated README.md with overall score: ${overallScore}%`);
            }

            // Post GitHub Commit Status
            const targetSha = context.sha;
            const state = allPassed ? 'success' : 'failure';
            const description = `Score: ${overallScore}% (${packages.filter(p => p.passed_break || p.passed).length}/${packages.length} packages >= ${breakThreshold}%) - ${statusLabel}`;

            console.log(`Publishing commit status for ${targetSha}: state=${state}, description="${description}"`);

            try {
              await github.rest.repos.createCommitStatus({
                owner: context.repo.owner,
                repo: context.repo.repo,
                sha: targetSha,
                state: state,
                context: 'mutation-testing/stryker',
                description: description.substring(0, 140),
                target_url: `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`
              });
              console.log('✅ Successfully created commit status mutation-testing/stryker');
            } catch (err) {
              console.error('⚠️ Failed to create commit status:', err.message);
            }

            if (!allPassed) {
              core.setFailed(`Consolidated Stryker Mutation Testing Gate failed: score=${overallScore}%, passed=${allPassed}`);
            }

      - name: Commit and push updated README.md
        if: github.ref == 'refs/heads/main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          if git diff --staged --quiet; then
            echo "No changes to README.md to commit."
          else
            git commit -m "docs: update Stryker mutation testing metrics in README [skip ci]

          Overall Score: ${{ steps.eval-gate.outputs.overall_score || '100.00' }}%
          Run: ${{ github.run_id }}"
            git push origin main
          fi
